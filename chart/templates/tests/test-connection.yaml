apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goimgupload.fullname" . }}-test-connection"
  labels:
{{ include "goimgupload.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "goimgupload.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
