apiVersion: v1
kind: Service
metadata:
  name: {{ include "goimgupload.fullname" . }}
  labels:
    {{- include "goimgupload.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goimgupload.selectorLabels" . | nindent 4 }}
